"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getServerSideProps = void 0;
const head_1 = __importDefault(require("next/head"));
const router_1 = require("next/router");
const dynamic_1 = __importDefault(require("next/dynamic"));
const example_1 = require("~/server/api/routers/example");
const GameConnectDynamic = (0, dynamic_1.default)(() => {
    return Promise.resolve().then(() => __importStar(require('../components/game-connect')));
}, {
    ssr: false,
});
const getServerSideProps = async (context) => {
    const { slug } = context.query;
    if (typeof slug !== 'string') {
        return {
            props: {},
        };
    }
    const game = await (0, example_1.getGame)(slug);
    if (!game) {
        return {
            props: {},
        };
    }
    return {
        props: {
            seo: {
                state: game.state,
            },
        },
    };
};
exports.getServerSideProps = getServerSideProps;
const GamePage = (props) => {
    var _a;
    const router = (0, router_1.useRouter)();
    const { slug } = router.query;
    if (!slug || typeof slug !== 'string')
        return null;
    return (<>
            <head_1.default>
                <title>A game with state {(_a = props.seo) === null || _a === void 0 ? void 0 : _a.state}</title>
                <meta name="description" content="Generated by create-t3-app"/>
                <link rel="icon" href="/favicon.ico"/>
            </head_1.default>
            <main>
                <GameConnectDynamic slug={slug}/>
            </main>
        </>);
};
exports.default = GamePage;
